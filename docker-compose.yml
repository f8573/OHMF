services:
  contacts:
    build:
      context: .
      dockerfile: ohmf/services/contacts/Dockerfile
    container_name: ohmf_contacts
    expose:
      - "18085"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:18085/healthz >/dev/null 2>&1 || exit 1"]
      interval: 5s
      timeout: 3s
      retries: 5

  apps:
    build:
      context: .
      dockerfile: ohmf/services/apps/Dockerfile
    container_name: ohmf_apps
    environment:
      - APP_MIGRATIONS_DIR=/opt/ohmf/apps/migrations
    expose:
      - "18086"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:18086/healthz >/dev/null 2>&1 || exit 1"]
      interval: 5s
      timeout: 3s
      retries: 5

  media:
    build:
      context: .
      dockerfile: ohmf/services/media/Dockerfile
    container_name: ohmf_media
    expose:
      - "18087"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:18087/healthz >/dev/null 2>&1 || exit 1"]
      interval: 5s
      timeout: 3s
      retries: 5

  gateway:
    build:
      context: ./ohmf/services/gateway
      dockerfile: Dockerfile
    container_name: ohmf_gateway
    expose:
      - "8081"
    environment:
      - APP_ADDR=:8081
      - APP_SMOKE_MODE=1
      - APP_LOG_LEVEL=debug
      - APP_CONTACTS_ADDR=http://contacts:18085
      - APP_APPS_ADDR=http://apps:18086
      - APP_MEDIA_ADDR=http://media:18087
    depends_on:
      contacts:
        condition: service_healthy
      apps:
        condition: service_healthy
      media:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:8081/healthz >/dev/null 2>&1 || exit 1"]
      interval: 5s
      timeout: 3s
      retries: 10

  itest:
    image: golang:1.25-alpine
    container_name: ohmf_itest
    working_dir: /src
    volumes:
      - ./:/src:ro
    depends_on:
      contacts:
        condition: service_healthy
      apps:
        condition: service_healthy
      media:
        condition: service_healthy
      gateway:
        condition: service_healthy
    environment:
      - INTEGRATION=1
      - GATEWAY_ADDR=http://gateway:8081
      - CONTACTS_ADDR=http://contacts:18085
    command: sh -c "apk add --no-cache curl ca-certificates >/dev/null 2>&1 || true; sleep 3; cd ohmf; INTEGRATION=1 GATEWAY_ADDR=http://gateway:8081 CONTACTS_ADDR=http://contacts:18085 go test ./integration -run TestGatewayIntegration -v"

  postgres:
    image: postgres:15-alpine
    container_name: ohmf_postgres
    environment:
      - POSTGRES_USER=dev
      - POSTGRES_PASSWORD=dev
      - POSTGRES_DB=dev
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "dev"]
      interval: 5s
      timeout: 3s
      retries: 10
    volumes:
      - ./postgres-data:/var/lib/postgresql/data
