apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: ohmf

# Optional autoscaling baseline layered on top of the full local k3s pipeline.
#
# This is intentionally a LOCAL smoke profile:
# - it assumes Metrics Server is available
# - it targets the gateway only
# - its threshold is tuned for visible behavior on a single-node dev cluster
# - it does not imply production HPA tuning

resources:
  - ../local-k3s-full
  - hpa.yaml
